import { Routes } from '@angular/router';

import { Library } from './library';

export const LIBRARY_ROUTES: Routes = [
  {
    path: '',
    component: Library,
    title: 'Vault Keeper | Library',
  },
];
